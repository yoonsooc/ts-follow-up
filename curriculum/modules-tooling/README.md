# 카테고리: modules-tooling — 모듈 시스템과 도구 지형 (미착수)

ESM vs CJS(TS보다 큰 고통의 근원), tsconfig 심화, 번들러 지형(esbuild/Vite/bun),
모노레포. "왜 import가 안 되지"의 90%가 여기서 발생. TS 7(tsgo) 전환기라 더욱 중요.

이 저장소에서 이미 실전으로 겪은 것: TS 7의 `@types` 자동 포함 제거(`types` 명시),
TS5112(파일 지정 시 tsconfig 미로드), 실행기(bun)와 검사기(tsc)의 분리.

## 세션 목록

(미정 — 착수 시 구성)
