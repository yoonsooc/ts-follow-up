# 카테고리: async — 비동기 모델과 동시성 (미착수)

이벤트 루프, Promise 조합(`all`/`allSettled`/`race`/`any`), async 함수의 에러 전파,
`AbortController`, 동시성 제어(세마포어/큐).

Go의 고루틴/채널, Rust의 tokio와 **완전히 다른 단일 스레드 모델** — 백엔드 감각이
가장 크게 배신당하는 영역이라 types 다음 1순위 후보.

## 세션 목록

(미정 — 착수 시 구성)
