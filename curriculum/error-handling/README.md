# 카테고리: error-handling — 에러 처리 아키텍처 (미착수)

Error 서브클래싱, `cause` 체인, throw vs Result 설계, 전역 에러 경계.
Java의 checked exception도 Rust의 `?`도 없는 세계에서의 설계 —
types S4(총함수)·S5(safeParse) 논의의 확장.

types 백로그의 "Result 패턴 직접 설계" 세션과 겹치는 영역: 타입 설계 관점이면
types에서, 아키텍처 관점이면 여기서 다룬다.

## 세션 목록

(미정 — 착수 시 구성)
